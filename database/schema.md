# Shoplytics Relational Schema & Entity-Relationship Model

## Database Overview
- **Database Engine**: PostgreSQL 17
- **Database Name**: `shoplytics`
- **Design Pattern**: Serving & Analytical Layer for E-Commerce Big Data Platform

---

## Entity-Relationship Diagram (Mermaid)

```mermaid
erDiagram
    CUSTOMERS ||--o{ ORDERS : places
    CUSTOMERS ||--o| CUSTOMER_SEGMENTS : segmented_as
    CUSTOMERS ||--o{ RECOMMENDATIONS : receives
    PRODUCTS ||--o{ RECOMMENDATIONS : recommended_in
    PRODUCTS ||--o{ ANALYTICS : contains_item
    ORDERS ||--o{ ANALYTICS : recorded_in
    CUSTOMER_SEGMENTS }|--|| CLUSTER_PROFILES : belongs_to

    CUSTOMERS {
        varchar(64) customer_unique_id PK
        int total_orders
        numeric total_spending
        numeric total_freight
        int total_products
        numeric average_order_value
        int cluster
    }

    PRODUCTS {
        varchar(64) product_id PK
        varchar(100) product_category_name
        int product_name_lenght
        int product_description_lenght
        int product_photos_qty
        numeric product_weight_g
        numeric product_length_cm
        numeric product_height_cm
        numeric product_width_cm
        varchar(100) product_category_name_english
    }

    ORDERS {
        varchar(64) order_id PK
        varchar(64) customer_id
        varchar(50) order_status
        timestamp order_purchase_timestamp
        timestamp order_approved_at
        timestamp order_delivered_carrier_date
        timestamp order_delivered_customer_date
        timestamp order_estimated_delivery_date
        int order_year
        int order_month
        varchar(20) order_month_name
        date order_date
        int order_day
        varchar(20) order_day_of_week
        numeric delivery_days
        numeric estimated_delivery_days
    }

    ANALYTICS {
        bigserial id PK
        varchar(64) order_id
        int order_item_id
        varchar(64) product_id
        varchar(64) seller_id
        timestamp shipping_limit_date
        numeric price
        numeric freight_value
        numeric total_item_value
        varchar(64) customer_id
        varchar(50) order_status
        timestamp order_purchase_timestamp
        timestamp order_approved_at
        timestamp order_delivered_carrier_date
        timestamp order_delivered_customer_date
        timestamp order_estimated_delivery_date
        int order_year
        int order_month
        varchar(20) order_month_name
        date order_date
        int order_day
        varchar(20) order_day_of_week
        numeric delivery_days
        numeric estimated_delivery_days
        varchar(100) product_category_name_english
        varchar(64) customer_unique_id
        varchar(100) customer_city
        varchar(10) customer_state
    }

    CUSTOMER_SEGMENTS {
        varchar(64) customer_unique_id PK
        int total_orders
        numeric total_spending
        numeric total_freight
        int total_products
        numeric average_order_value
        int cluster
    }

    CLUSTER_PROFILES {
        int cluster PK
        bigint customers
        numeric avg_orders
        numeric avg_spending
        numeric avg_freight
        numeric avg_products
        numeric avg_order_value
        numeric min_spending
        numeric max_spending
    }

    RECOMMENDATIONS {
        bigserial id PK
        varchar(64) customer_id
        int rank
        varchar(64) product_id
        varchar(100) product_category
        numeric recommendation_score
        numeric apriori_score
        numeric similarity_score
        numeric popularity_score
        varchar(100) recommendation_source
    }

    ASSOCIATION_RULES {
        bigserial id PK
        text antecedent
        text antecedent_categories
        text consequent
        text consequent_categories
        numeric antecedent_support
        numeric consequent_support
        numeric support
        int support_count
        numeric confidence
        numeric lift
    }
```

---

## Table Relationships & Cardinality

1. **`customers` to `orders`**: 1-to-many relationship (one unique customer can place multiple orders).
2. **`customers` to `customer_segments`**: 1-to-1 relationship linking customer profile to their assigned K-Means cluster.
3. **`cluster_profiles` to `customer_segments`**: 1-to-many relationship mapping cluster IDs to their segment profile aggregate stats.
4. **`customers` / `products` to `recommendations`**: Personalized recommendations generated per customer referencing recommended products.
5. **`analytics`**: Denormalized analytical line-item data combining order transactions, items, categories, customer locations, and fulfillment metrics.
6. **`association_rules`**: Cross-product association rules generated by Apriori algorithm with support, confidence, and lift metrics.
